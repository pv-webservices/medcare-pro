/**
 * WhatsApp Cloud API (Meta) wrapper — PRD §6.5 (FR-5.1 … FR-5.4).
 *
 * SCAFFOLD ONLY. Not implemented yet; wired up in the WhatsApp stage.
 * Planned shape:
 *  - Send pre-approved template messages only (PRD §10 Compliance — no free text)
 *  - Reads WHATSAPP_TOKEN / WHATSAPP_PHONE_ID from the environment
 *  - Verifies inbound webhook signatures before processing (PRD §10 Security)
 */

export {};

// Roles — PRD §6.8 (FR-8.1, FR-8.2): create roles, assign with optional clinic scope. SCAFFOLD ONLY.

export default function RolesSettingsPage() {
  return (
    <section>
      <h1>Roles & permissions</h1>
      <p>Not implemented yet.</p>
    </section>
  );
}

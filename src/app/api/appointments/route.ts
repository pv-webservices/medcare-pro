import { notImplemented } from "@/lib/utils";

// PRD §9 — Appointment CRUD (FR-3.1 … FR-3.3).
// TODO(appointments stage): list with date-range filter, create, update, delete.
// Requires an authenticated session (PRD §10 Security).

export async function GET() {
  return notImplemented("GET /api/appointments");
}

export async function POST() {
  return notImplemented("POST /api/appointments");
}

export async function PUT() {
  return notImplemented("PUT /api/appointments");
}

export async function DELETE() {
  return notImplemented("DELETE /api/appointments");
}
